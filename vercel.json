{
  "version": 2,
  "builds": [
    {
      "src": "build/**/*.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["build/**"] }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/$1" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
